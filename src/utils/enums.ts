export const enum Resource {
  Desert = 0,
  Wood,
  Wheat,
  Stone,
  Clay,
  Sheep,
}

export const enum Player {
  Red = 1,
  Yellow,
  Blue,
  White,
}

export const enum Building {
  Village = 1,
  City,
}
